<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Page</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/home.css">
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .header{
    width: 100%;
    height: 60px;
    display: flex;
    align-items: center;
    background-color: darkred;
    margin-bottom: 10px;
}
.header a{
    text-decoration: none;
    font-size: 25px;
    color: white;
    padding: 0 20px;
}
.header a:hover{
    font-weight: bold;
    color: black;
}
    </style>
</head>
<body>
    <div class="header">
        <div>
            <a href="/">HomePage</a>
            <a href="/view">View List User</a>
        </div>
        <div>
            <a href="/create">Create a User</a>
            <a href="/login">Login</a>
        </div>
    </div>

    
    <form style="display: flex; justify-content: center;" action="user/updateUser/<%= user._id %>" method="POST" >
        <div class="form " style=" width: 40%;display: flex;flex-direction: column; justify-content: center;">
            <h2 style="text-align: center">Form update  </h2>
            <input style="padding: 10px 20px; margin: 10px 0; display: none;" type="text" name="_id" disabled value="<%= user._id %>">
            <input style="padding: 10px 20px; margin: 10px 0;" type="text" name="email" placeholder="email" value="<%= user.email %>">
            <input style="padding: 10px 20px; margin: 10px 0;" type="text" name="name" placeholder="name" value="<%= user.name %>">
            <input style="padding: 10px 20px; margin: 10px 0;" type="text" name="password" placeholder="password" value="<%= user.password %>">
            <input style="padding: 10px 20px; margin: 10px 0;" type="text" name="phone" placeholder="phone" value="<%= user.phone %>">
            <input style="padding: 10px 20px; margin: 10px 0;" type="text" name="city" placeholder="city" value="<%= user.city %>">
            <input style="padding: 10px 20px; margin: 10px 0;" class="button" type="submit" placeholder="submit" >
        </div>
    </form>
</body>
</html>